コンテナへ接続
docker-compose exec python3 bash
docker-compose up --build

cd opt
python rand_array.py

docker-compose exec python3 PythonStart
docker-compose exec python3 bash -c "/root/PythonStart 'rand_array.py'"

docker-compose exec python3 /root/PythonStart
bin/DockerPythonStart basic.py
bin/DockerPythonStart

__pycache__
pythonファイルを実行したとき、コンパイルされたモジュールが__pycache__の中pycファイルにキャッシュされます。

docker-compose exec sqlite bash -c "sqlite3"
.exit
.open FILENAME

    command: >
      bash -c 'pip install --upgrade pip &&
      pip install selenium &&
      tail -f /dev/null'

    command:
      sh -c 'echo "Konnichiwa!" > /shared_dir/hello.txt; chmod 600 /shared_dir/hello.txt'

    depends_on:
      - sqlite

command: pip install -r /root/opt/requirements.txt
docker-compose exec python3 bash -c "pip install -r /root/opt/requirements.txt"

ModuleNotFoundError: No module named 'numpy'

RUN wget --quiet http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz -O ta-lib-0.4.0-src.tar.gz && \
    tar xvf ta-lib-0.4.0-src.tar.gz && \
    cd ta-lib/ && \
    ./configure --prefix=/usr && \
    make && \
    sudo make install && \
    cd .. && \
    pip install TA-Lib && \
    rm -R ta-lib ta-lib-0.4.0-src.tar.gz

bin/DockerPythonStart main.py

-a　配列変数に値をセットする

bin/DockerPythonStart cryptocurrency_blockchain.py

docker-compose exec web bash -c "pip install -r /root/opt/requirements.txt"

pip list
      - ./pip-lib:/usr/local/lib/python3.8/site-packages/
      - ./pychace:/root/pycache/root/opt/

sudo docker cp 4b468148f4d0:/usr/local/lib/python3.8/site-packages/ ./pip-lib/
pip install flask flask_sqlalchemy
docker-compose exec web bash -c "pip install -r /root/opt/requirements.txt"
docker-compose exec web bash -c "pip install flask_sqlalchemy"
/root/.local/lib/python3.8/site-packages
docker cp 4b468148f4d0:/root/.local/lib/python3.8/site-packages/ ./pip-lib2/
docker-compose exec web bash -c "python main.py"
http://localhost:8080
docker-compose exec web bash -c "ls /usr/local/lib/python3.8/site-packages/"
sqlite3.OperationalError: no such table: todos

docker-compose exec web bash -c "pip install requests"

return jsonify({'content': 'Hello, world'})
pip install -U flask-cors
docker-compose exec web bash -c "pip install flask-cors"

  server:
    image: nginx
    depends_on:
      - web
      - bot
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80















